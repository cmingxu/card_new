.w96
  .r_bt
    .r_bt_l
      .r_bt_l_a
      .r_bt_l_c.fb.white 数据备份
    .r_bt_r
      %button.submit2.hand{:onclick => "window.location.href='/welcome/backup_db'", :type => "button"} 现在备份
  .table
    %ul.bttitle.black.fb
      %li.w5 编号
      %li.w20 时间
      %li.w20 文件大小
      %li.w30 存储地址
      %li.w15 操作
    - @backups.each_with_index do |backup,index|
      - backup = Pathname.new(backup)
      %ul.table_items
        %li.w5= index + 1
        %li.w20= backup.ctime.strftime("%Y-%m-%d %H:%M")
        %li.w20= number_to_human(backup.size, :units => {:unit => "", :thousand => "KB"})
        %li.w30= backup.basename.expand_path.to_s
        %li.w15
          = link_to '删除', welcome_delete_backup_path(:name => backup.basename.expand_path.to_s), :confirm => '确定删除？', :method => :delete
  .h10
  .h10
  .h10
  .h10
